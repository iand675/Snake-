-- Used for building within the Cabal packaging framework
-- Run from the commandline with:
-- runhaskell Setup.hs configure
-- runhaskell Setup.hs build
-- (sudo) runhaskell Setup.hs install
import Distribution.Simple
main = defaultMain